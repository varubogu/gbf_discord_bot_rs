# 起動時検証

## 概要

起動時に「設定不足」「必須データ不足」「接続不可」などを検知し、運用上の事故を減らすための仕組みです。

## 代表ユースケース

- DB接続に失敗しているのに、起動したように見えてしまう事故を防ぐ
- 必須の環境変数が欠けている場合に、早期に気づけるようにする

## 制約

- 検証は起動を遅くしすぎない（重い外部I/Oを増やさない）
- 検証失敗時のメッセージは、運用者が次に何をすべきか分かる形にする

