# タイムゾーン設定

## 概要

ユーザーが入力する日時や通知時刻を、サーバー/ユーザーのタイムゾーンに合わせて扱うための機能です。

## 基本方針

- DB保存はUTC、表示は利用者が理解できるタイムゾーン
- 入力は「何を基準に解釈するか」を決めてからパースする（曖昧さを残さない）

## 制約

- サーバーごとのデフォルトタイムゾーンと、ユーザー入力を混ぜて解釈しない
- 相対表現（例: 30分後）を許容する場合は、基準時刻の扱いを明確にする

