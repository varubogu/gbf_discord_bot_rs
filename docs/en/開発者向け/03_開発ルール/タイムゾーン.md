# タイムゾーン

日時入力や通知はタイムゾーンの扱いが複雑になりやすい領域です。

## 最低限のルール

- タイムゾーンについて、プログラム上とDB保存はUTCを基本とし、ユーザー入力と表示はDiscordサーバー（Guild）のタイムゾーンで行う（event層で変換する）
- 「ユーザーが入力した日時」がどのタイムゾーン基準かを曖昧にしない
- `now()` に依存する処理はテストしづらいので、基準時刻を引数で渡せる形を検討する
