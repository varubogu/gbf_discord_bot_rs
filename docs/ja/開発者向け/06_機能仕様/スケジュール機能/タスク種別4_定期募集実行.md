# タスク種別4: 定期募集実行（RecurringRecruitment）

## 概要

`task_type = 4` は定期募集スケジュールを実行し、募集を自動作成するタスクです。  
`scheduled_task_recurring_recruitments` で対象スケジュールと紐づけて管理します。

## 登録・削除フロー

### 登録

- スケジュール作成時（`ScheduleCreateService`）
- スケジュール有効化時（`ScheduleCommandService::enable_schedule`）
- 実行完了時の次回予約（`RecurringRecruitmentTaskExecutor::create_next_scheduled_task`）

登録時は以下2テーブルを同時に更新します。

1. `scheduled_tasks`（`task_type=4`）
2. `scheduled_task_recurring_recruitments`（`scheduled_task_id` ↔ `recruitment_schedule_id`）

### 削除

- スケジュール無効化・削除時に pending タスクのみ削除
- 関連の `scheduled_task_recurring_recruitments` も削除

## 実行フロー

`SchedulerManager` の `task_type=4` 分岐で `RecurringRecruitmentTaskExecutor` を実行します。

1. タスク存在と `pending` を再確認
2. `scheduled_task_recurring_recruitments` から対象スケジュールIDを取得
3. スケジュール本体（曜日含む）を取得
4. スケジュール未存在/無効化時は `succeeded_with_warning` で終了
5. `task.schedule_datetime` から当該実行回の `CalculatedRecruitmentTime` を復元
6. `quest_start_at <= now` の場合は募集作成をスキップし、次回実行タスクのみ再登録して現在タスクを `succeeded_with_warning` に更新
7. 出発時刻が未来の場合のみ `RecruitmentCreationService::create_recruitment_from_schedule` で募集作成
8. 次回実行タスクを計算・登録（最大365日先まで探索）
9. 募集作成した場合の現在タスクは `succeeded`

## 実装上の注意

- 募集作成時刻は `task.schedule_datetime` に対応する `CalculatedRecruitmentTime` から復元し、`Utc::now()` を直接代入しません。
- Bot停止などで実行が遅延し、実行時点で `quest_start_at <= now` の場合は募集作成を行いません。
- 次回実行日時は7日刻みで探索し、最初に見つかった未来日時のみ登録します。

## 実行状態とエラー制御

- スケジュール削除済み: `succeeded_with_warning`
- スケジュール無効化済み: `succeeded_with_warning`
- 正常完了: `succeeded`
- 実行中エラー: `SchedulerManager` 側で `failed`

## 関連テーブル

- `worker.scheduled_tasks`
- `worker.scheduled_task_recurring_recruitments`
- `guild_master.battle_recruitment_schedules`
- `guild_master.battle_recruitment_schedule_days`
- `worker.battle_recruitments`

## 関連ドキュメント

- [../スケジュール機能.md](../スケジュール機能.md)
- [../定期募集.md](../定期募集.md)
- [../../05_データベース/スキーマ/worker/scheduled_tasks.md](../../05_データベース/スキーマ/worker/scheduled_tasks.md)
- [../../05_データベース/スキーマ/worker/scheduled_task_recurring_recruitments.md](../../05_データベース/スキーマ/worker/scheduled_task_recurring_recruitments.md)
- [../../05_データベース/スキーマ/guild_master/battle_recruitment_schedules.md](../../05_データベース/スキーマ/guild_master/battle_recruitment_schedules.md)
- [../../05_データベース/スキーマ/guild_master/battle_recruitment_schedule_days.md](../../05_データベース/スキーマ/guild_master/battle_recruitment_schedule_days.md)
