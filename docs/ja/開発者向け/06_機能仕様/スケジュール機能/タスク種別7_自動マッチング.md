# タスク種別7: 自動マッチング（AutoMatching）

## 概要

`task_type = 7` は自動募集参加者の希望条件をマッチングし、  
成立グループの通知と募集作成を行うタスクです。  
対象は全ギルドで、`scheduled_tasks.guild_id` / `channel_id` は `NULL` で管理します。

## 登録フロー

- 初回登録: 自動募集カテゴリ登録時（`category_setup_facade`）
- 再登録: 実行完了時に「10秒後」を次回として自己再登録

初回登録時は pending タスクを確認し、`task_type=7` が既にある場合は重複作成しません。

## 実行フロー

`SchedulerManager` の `task_type=7` 分岐で `AutoMatchingTaskExecutor` を実行します。

1. タスク存在と `pending` を再確認
2. `PeriodicMatchingService::process_matching` を実行
   - 参加可能時刻 (`auto_recruitment_participants`) と希望クエスト (`user_desired_quests`) を結合
   - 同一条件をグルーピングし、2人以上を候補化
   - 6属性クエストは属性重複を避けるグルーピングを適用
   - `quest_matchings` / `quest_matching_users` を作成
3. 成立マッチングごとに通知送信と募集作成を試行
4. 現在タスクを `succeeded`
5. 次回タスク（10秒後）を作成

## 通知・募集作成の詳細

成立マッチング単位で以下を実行します。

1. ギルド設定 (`auto_recruitments`) とマッチングチャンネルを解決
2. 参加者メンション付き通知を送信（Embed）
3. 月日時間から出発時刻（JST基準、`24..28時` 表記対応）を計算
4. 出発時刻が未来なら募集を作成し、`quest_matchings.recruitment_id` を更新

## 実行状態とエラー制御

- マッチング成立が0件でも `succeeded`（`NoMatches`）
- 通知送信失敗はログ出力のみで募集作成を継続
- 募集作成失敗は該当マッチングのみ失敗として継続
- 実行中の致命的エラーは `SchedulerManager` 側で `failed`

## 実装上の注意

- 出発時刻が過去になったマッチングは募集作成をスキップします。
- クエスト情報や自動募集設定が取得できないギルド/マッチングはスキップします。
- 自己再登録方式のため、タスクチェーンが継続していることを監視対象に含める前提です。

## 関連テーブル

- `worker.scheduled_tasks`
- `guild_master.auto_recruitments`
- `guild_master.auto_recruitment_participants`
- `guild_master.user_desired_quests`
- `worker.quest_matchings`
- `worker.quest_matching_users`
- `worker.battle_recruitments`

## 関連ドキュメント

- [../スケジュール機能.md](../スケジュール機能.md)
- [../自動募集.md](../自動募集.md)
- [../../05_データベース/スキーマ/worker/scheduled_tasks.md](../../05_データベース/スキーマ/worker/scheduled_tasks.md)
- [../../05_データベース/スキーマ/guild_master/auto_recruitments.md](../../05_データベース/スキーマ/guild_master/auto_recruitments.md)
- [../../05_データベース/スキーマ/guild_master/auto_recruitment_participants.md](../../05_データベース/スキーマ/guild_master/auto_recruitment_participants.md)
- [../../05_データベース/スキーマ/guild_master/user_desired_quests.md](../../05_データベース/スキーマ/guild_master/user_desired_quests.md)
- [../../05_データベース/スキーマ/worker/quest_matchings.md](../../05_データベース/スキーマ/worker/quest_matchings.md)
- [../../05_データベース/スキーマ/worker/quest_matching_users.md](../../05_データベース/スキーマ/worker/quest_matching_users.md)
