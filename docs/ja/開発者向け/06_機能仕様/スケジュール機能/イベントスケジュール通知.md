# イベントスケジュール通知

## 概要

本書は、`task_type = Notification`（イベントスケジュール通知）に特化した仕様を定義します。  
スケジューラーの共通基盤（実行状態、実行サイクル、整合性方針など）は [../スケジュール機能.md](../スケジュール機能.md) を参照してください。

## global / guild 統合仕様

イベント通知を再生成するときの `global` と `guild` の統合ルールを定義します。

### スケジュール生成に使用するテーブル

イベント通知の生成時に参照するテーブルは次の6つです。

1. `master.event_schedules`（グローバルのイベントスケジュール）
2. `master.event_schedule_details`（グローバルのイベント期間内詳細スケジュール）
3. `master.message_texts`（グローバルのメッセージテキスト）
4. `guild_master.guild_event_schedules`（guild版イベントスケジュール）
5. `guild_master.guild_event_schedule_details`（guild版イベント期間内詳細スケジュール）
6. `guild_master.guild_message_texts`（guild版メッセージテキスト）

### global / guild の同一判定

- `event_schedules` 系: `id` が同じ場合に同一スケジュールとして扱う
- `event_schedule_details` 系: `id` が同じ場合に同一詳細として扱う
- `message_texts` 系: `id` が同じ場合に同一メッセージとして扱う
- ただしイベントスケジュールと詳細の結合は、`profile` が同一かで判断し、イベントスケジュールと詳細の `id` は結合条件としない

### 統合方針

- globalのみ存在: globalデータで通知を生成する
- guildのみ存在: guildデータのみで通知を生成する
- globalとguildの両方が存在: 同一 `id` の範囲でguild側を優先して上書きする

## 解決ルール

### 通知先チャンネル解決処理の優先順位

通知先チャンネルは次の順で解決します。

1. `guild_event_schedule_details.notification_channel_id`
2. `guild_event_schedule_details.notification_channel_type` から `guild_channels` を解決
3. `event_schedule_details.notification_channel_type` から `guild_channels` を解決
4. 1〜3のいずれでも解決できない場合はエラー

前提:

- `guild_event_schedule_details.notification_channel_id` はNULL許容

補足:

- guild詳細で `notification_channel_id` と `notification_channel_type` の両方が設定されている場合は、`notification_channel_id` を優先する
- guild詳細で両方未設定の場合、そのguild向けの当該詳細通知は登録しない（警告ログを出して継続）

### メッセージテキストID解決処理の優先順位

通知行に紐づける `message_text_id` は次の順で解決します。

1. guild詳細に `message_text_id` が定義されていればそれを使用
2. global詳細に `message_text_id` が定義されていればそれを使用
3. 1,2のどちらも解決できない場合はエラー

`message_text_id` 決定後の本文解決（`MessageService` 利用、`guild_message_texts` -> `message_texts` -> `locales/messages.yml` -> エラー）とロケール選択は、[../メッセージ解決.md](../メッセージ解決.md) の仕様に従う。

## スケジュール再生成フロー（通知）

1. 既存の通知系スケジュール（`worker.scheduled_tasks` / `worker.notifications` / `worker.notification_rel_event_schedules`）を初期化
2. global / guild の `event_schedules` を統合
3. global / guild の `event_schedule_details` を統合
4. 統合結果をもとに、guildごとに通知先チャンネルと `message_text_id` を解決
5. 解決できた行のみ `scheduled_tasks` / `notifications` / `notification_rel_event_schedules` を作成
6. エラー行は記録して継続し、処理全体の完了後に集計結果を出力

## 再生成コマンドとパラメータ

### Event層 / Facade層の呼び分け

- guild用コマンド: 対象guildに関わるスケジュールのみ再生成する
- 管理サーバー用コマンド: 全guildのスケジュールを再生成する
- どちらも `task_type` を任意で受け付ける（未指定時は全タスク種別を対象とする）

### Service層の入口（Facadeからの呼び出し）

- guild用入口: `guild_id`（必須）+ `task_type`（任意）
  - 利用DBロール: `guild`
- 管理サーバー用入口: `task_type`（任意）
  - 利用DBロール: `global`

### Service内側の対象解決

- `guild_id` が指定されていればそのguildのみを対象とする
- `guild_id` が未指定なら全guildを対象とする
- `task_type` が指定されていればその種別のみを対象とする
- `task_type` が未指定なら全タスク種別を対象とする

### スプレッドシート読み込み時の自動再生成

- `/gspread_global_load` 後の自動再生成は、管理サーバー用再生成と同じ扱いで実行する
  - 対象guild: 全guild
  - 対象タスク種別: `Notification`（`task_type=1`）のみ
- `/gspread_load` 後の自動再生成は、guild用再生成と同じ扱いで実行する
  - 対象guild: コマンド実行guildのみ
  - 対象タスク種別: `Notification`（`task_type=1`）のみ
- つまり `/gspread_load` 後の挙動は、guild用スケジュール再生成コマンドで `task_type=1` を指定した場合と同一とする

補足（`notification_rel_event_schedules` への関連付け）:

- `event_schedule_id` は `master.event_schedules.id` に存在する場合のみ関連付ける
- `event_schedule_detail_id` は `master.event_schedule_details.id` に存在する場合のみ関連付ける
- guild独自ID（`guild_event_schedules` / `guild_event_schedule_details` のみ存在するID）の場合は、
  `scheduled_tasks` / `notifications` は作成するが、`notification_rel_event_schedules` の該当カラムは未設定（NULL）または行作成なしとなる

## イベント通知のエラー制御

### 基本方針

- どのデータで何のエラーが発生したかを識別可能な形で記録する
- エラー行以外のスケジュール生成は継続する

### エラー単位

- `event_schedules`（global / guild）起因: イベントスケジュール行単位
- `event_schedule_details`（global / guild）起因: イベント詳細行単位
- メッセージ未定義起因: イベント詳細行単位
- 通知先未解決起因: イベント詳細行単位（guild単位の文脈を含める）

### エラー時に保持する情報

- `guild_id`（該当する場合）
- `event_schedule_id`
- `event_schedule_detail_id`
- `message_text_id`（解決途中で判明している場合）
- エラー種別（例: `NotificationChannelNotResolved`, `MessageTextNotResolved`）

## パターン一覧（正常）

※ ここで使用している数字（1〜6）は[スケジュール生成に使用するテーブル](#スケジュール生成に使用するテーブル)の番号を指します。  
※ 各パターンの記載順は「イベント → 詳細 → メッセージ」です。

- 1,2,3あり、4,5,6なし
  - globalのみのパターン
  - 1,2の日時・通知先に対して3を送信

- 1,2,3,6あり、4,5なし
  - イベント情報はglobal、メッセージのみguildのパターン
  - 1,2の日時・通知先に対して6を送信

- 1,2,3,4,5,6あり
  - guildがglobalを完全に上書きしているパターン
  - 4,5の日時・通知先に対して6を送信

- 1,2,3,4,5あり、6なし
  - guildがglobalを上書きしているが、メッセージテキストはglobalのパターン
  - 4,5の日時・通知先に対して3を送信

- 1,5,3あり、4,2,6なし
  - イベント基準日はglobalだが、内部スケジュールをguildで定義し、メッセージはglobalのパターン
  - 1のイベント日から見た5の日時・通知先に対して3を送信

- 1,5,6あり、2,4,3なし
  - イベント基準日はglobalだが、内部スケジュールをguildで定義し、メッセージもguildのパターン
  - 1のイベント日から見た5の日時・通知先に対して6を送信

- 4,5,6あり、1,2,3なし
  - guild独自のイベントで通知を行うパターン
  - 4,5の日時・通知先に対して6を送信

## パターン一覧（エラー）

- 2または5で決定した `message_text_id` に対応する本文が6,3,`locales/messages.yml` のいずれでも未定義
  - メッセージ内容を解決できないパターン
  - [../メッセージ解決.md](../メッセージ解決.md) を参照
  - メッセージ未解決エラーとして詳細行単位で記録されること

- 2または5（通知先チャンネルID指定なし）でチャンネル種別ID指定があるが、そのguildで対応する `guild_channels` の行が存在しない
  - 通知先チャンネルが解決できないパターン
  - [通知先チャンネル解決処理の優先順位](#通知先チャンネル解決処理の優先順位) を参照
  - 解決できるguildのみ通知登録し、解決不能guildはエラー記録して継続すること

## 関連ドキュメント

- [../スケジュール機能.md](../スケジュール機能.md)
- [../募集通知.md](../募集通知.md)
- [../メッセージ解決.md](../メッセージ解決.md)
- [../../05_データベース/スキーマ/master/event_schedules.md](../../05_データベース/スキーマ/master/event_schedules.md)
- [../../05_データベース/スキーマ/master/event_schedule_details.md](../../05_データベース/スキーマ/master/event_schedule_details.md)
- [../../05_データベース/スキーマ/guild_master/guild_event_schedules.md](../../05_データベース/スキーマ/guild_master/guild_event_schedules.md)
- [../../05_データベース/スキーマ/guild_master/guild_event_schedule_details.md](../../05_データベース/スキーマ/guild_master/guild_event_schedule_details.md)
- [../../05_データベース/スキーマ/master/message_texts.md](../../05_データベース/スキーマ/master/message_texts.md)
- [../../05_データベース/スキーマ/guild_master/guild_message_texts.md](../../05_データベース/スキーマ/guild_master/guild_message_texts.md)
- [../../05_データベース/スキーマ/worker/scheduled_tasks.md](../../05_データベース/スキーマ/worker/scheduled_tasks.md)
