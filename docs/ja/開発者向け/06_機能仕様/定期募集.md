# 定期募集

## 概要

曜日・時刻ベースでマルチ募集を自動作成する機能です。
ギルドごとのスケジュールを `guild_master.battle_recruitment_schedules` に保存し、`task_type = RecurringRecruitment` として実行します。

## 目的

- 手動での定型募集作成コストを削減する
- ギルド運用に合わせた継続的な募集投稿を実現する
- スケジュール実行失敗時の再試行可能性を担保する

## 主要コマンド

- `/定期募集作成`
- `/定期募集削除`
- `/定期募集一覧`

実行権限は `gbf_bot_control` ロール保持者を前提とします。

## データモデル

### `guild_master.battle_recruitment_schedules`

- スケジュール本体（クエスト、戦術、時刻、有効フラグ）
- `recruit_start_day_offset` で募集開始日のオフセットを管理
- `is_enabled = false` で一時停止可能

### `guild_master.battle_recruitment_schedule_days`

- 実行曜日を管理（1スケジュールに複数曜日）

### `worker.scheduled_task_recurring_recruitments`

- 実行タスクとスケジュールの関連を保持

## オフセット仕様

`recruit_start_day_offset` の省略時は、時刻比較で自動決定します。

- `recruit_start_time < quest_start_time` の場合: `0`（当日募集）
- `recruit_start_time >= quest_start_time` の場合: `1`（前日募集）

手動指定時は入力値を優先します（実装上のバリデーション範囲に従う）。

## 処理フロー

1. スケジュール登録時に入力を検証し保存
2. SchedulerManager が対象時刻で `RecurringRecruitment` タスクを実行
3. 実行対象回の `quest_start_at` を `task.schedule_datetime` から復元
4. `quest_start_at <= now` の場合、現在が「次回候補の募集開始後〜出発前」ならその回を即時募集
5. 即時募集可能な回がなければ募集作成をスキップし、次回タスクのみ再登録して `succeeded_with_warning`
6. 出発時刻が未来の場合は募集メッセージを自動作成
7. 次回実行日時を計算し、次のタスクを再登録
8. `is_enabled = false` なら実行せずスキップ

## バリデーション

- 当日募集（offset=0）の場合、`recruit_start_time < quest_start_time` を要求
- 曜日未指定は禁止
- 存在しない `quest_id` / `battle_style_id` はエラー
- 募集チャンネル未設定時は作成を拒否

## エラーハンドリング

- 募集作成失敗時は当該実行を失敗として記録し、次回タスクとの整合性を維持
- Discord送信失敗時は再試行可能なログを残す
- スケジュール削除済みの孤立タスクは実行時再確認でスキップ
- 実行遅延で出発時刻が過去化したタスクは、必要に応じて現在募集可能な回を即時作成し、過去タスク自体は `succeeded_with_warning` で完了

## テスト観点

- オフセット自動判定ロジック
- 週次実行日時の計算（年跨ぎ含む）
- 無効化スケジュールのスキップ
- 次回タスク再登録の整合性

## 運用観点

- 有効スケジュール件数の監視
- 実行失敗率の監視
- 定期的な不要スケジュール棚卸し
