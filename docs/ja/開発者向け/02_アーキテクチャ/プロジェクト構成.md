# プロジェクト構成（開発者向け）

## 目的

「どこに何が置かれているか」を把握し、修正箇所を迷わないようにします。

## 重要ディレクトリ（概要）

```text
(root)
├── .tmp/                  # 一時作業領域（コミットしない）
├── db/                    # DBセットアップ用スクリプト / 初期データSQL
├── docs/                  # ドキュメント
├── locales/               # i18nメッセージ
├── migration/             # DBマイグレーション
├── src/
│   ├── bin/               # メンテナンス用バイナリ（例: cleanup）
│   ├── di/                # DI（依存性注入）/ 構成ルート
│   ├── events/            # プレゼンテーション層（Discord入力）
│   ├── facades/           # アプリケーション層（ユースケース + トランザクション境界）
│   ├── services/          # ドメイン/業務ロジック層
│   ├── repository/        # 永続化ポート層（trait + 入出力型のみ）
│   │   ├── auto_recruitment/
│   │   ├── schedule/
│   │   ├── *_repository.rs
│   │   └── mod.rs
│   ├── infrastructure/    # 外部システム具体実装（アダプター）
│   │   └── database/
│   │       ├── connection/      # DB接続管理
│   │       ├── session/         # DBセッション文脈（例: RLS用セッション変数）
│   │       ├── repositories/    # SeaORMによるRepositoryアダプター実装
│   │       │   ├── recruitment/
│   │       │   ├── schedule/
│   │       │   ├── auto_recruitment/
│   │       │   ├── guild/
│   │       │   ├── master_data/
│   │       │   └── ...
│   │       └── mod.rs
│   ├── gateway/           # Discord Gateway抽象
│   ├── models/            # ドメインモデル / エンティティモデル
│   ├── presenter/         # 表示変換（UIレスポンス構築）
│   ├── types/             # 共有型定義
│   ├── utils/             # 共通ユーティリティ
│   ├── lib.rs
│   └── main.rs
├── tests/
├── tools/
└── Cargo.toml
```

## RepositoryとInfrastructureの責務

- `src/repository/**` は **ポート層**。
  - Serviceが利用する永続化インターフェース（trait）を定義する。
  - ORM固有の詳細は含めない。
- `src/infrastructure/database/repositories/**` は **アダプター層**。
  - Repository traitをSeaORMで実装する。
  - SQL/ORM依存の最適化・実装詳細を保持する。

## import方針

- Service / Facade / Events では `src/repository/**` のポートを参照する。
- `SeaOrm*Repository` などの具象型は `src/di/repositories.rs` の配線でのみ扱う。
- `src/repository/**` に具象DBアダプターを配置しない。
