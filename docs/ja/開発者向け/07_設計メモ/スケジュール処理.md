# スケジュール処理

## 目的

- 「決まった時間に何かをする」を安定して実現する
- Bot停止中に実行できなかったタスクを、復帰後に取りこぼさない
- 二重実行を起こしにくくする

## 基本方針

- タスクはDBで管理し、実行直前にDBの最新状態を確認する
- 実行後は「実行済み」を記録し、重複を防ぐ
- トランザクションは短く保ち、外部I/O（Discord送信等）を抱え込まない

## 注意点

- 「通知」「解散」「データ整理」のように種類が増えるので、責務分割を崩さない
- 障害時は、ログから「どのタスクがどこで失敗したか」を追えるようにする

