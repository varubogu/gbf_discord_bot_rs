# ロギング

## 基本方針

- `tracing` を使い、構造化ログで出す
- 機密情報はログに出さない
- 例外（業務上の想定内）は `warn`、システム障害は `error`
- ログ専用文言（Discordへ送信しない文言）はコードへ直書きしてよい
## ログに入れると助かる情報

- `guild_id` / `channel_id` / `user_id`（可能な範囲）
- 何のユースケースか（例: 募集作成、スプレッドシート読み込み）
- 外部サービスの失敗（Discord/DB/Spreadsheet）がどこで起きたか
