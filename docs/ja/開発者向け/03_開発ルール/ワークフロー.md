# ワークフロー

## 目的

変更のたびに品質が下がらないよう、変更の流れを固定します。

## 最小フロー（推奨順）

1. 仕様/設計の更新（必要ならドキュメントから）
2. 実装
3. `cargo clippy` ※変更したファイルのみを対象にする
4. `cargo fmt` ※変更したファイルのみを対象にする
5. テスト（まず対象のコードを実行、最後に `cargo test` で全体実行）
