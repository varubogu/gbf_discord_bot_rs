# テスト補完計画（2026-02-20）

## 目的
単体テスト・結合テストの不足分岐を補完し、主要なキャンセル系フローの回帰検知力を向上させる。

## 前提
- `#[ignore]` が付与されているDB依存テストは、今回の対応でも `#[ignore]` を維持する。
- ignore解除やCI運用改善は別タスクとして扱う。

## 今回の実施対象
1. `tests/integration/facades/recruitment_cancel_test.rs`
2. `src/services/channel/admin_notification_service.rs`（同ファイル内 `#[cfg(test)]`）

## 追加するテストケース
### A. recruitment_cancel（結合テスト）
- `can_cancel` の未網羅分岐
  1. `MessageDeleted`（DBに募集あり + Discordメッセージ取得失敗）
  2. `NotRecruitMessage`（DBに募集なし + Discordメッセージ取得成功）
  3. `EventDatePassed`（DBに募集あり + 開催日時が過去 + Discordメッセージ取得成功）

### B. admin_notification_service（単体テスト）
- `notify_admin` の Repository 異常系
  1. `GuildChannelRepository::get_by_guild_and_type_with_txn` が `Err` を返したときに `Err` が返ること
  2. 上記ケースで `send_message` が呼ばれないこと

## 実施手順
1. 計画書（本ファイル）を作成
2. 対象テストを追加
3. `cargo fmt`
4. `cargo clippy -j 4`
5. 影響範囲のテスト実行
   - `cargo test -j 4 recruitment_cancel_test`
   - `cargo test -j 4 admin_notification_service`

## 完了条件
- 追加テストがコンパイルを通過する
- 追加テスト（非ignore対象）が成功する
- 既存 `#[ignore]` の有無を変更していない

## 既知の後続課題（今回スコープ外）
- events層コマンド（`recruit_cancel`, `auto_recruit_days_change`, `auto_recruit_category_unregister`）のUI分岐テスト整備
- ignoreテストを定期実行するための実行基盤整備
